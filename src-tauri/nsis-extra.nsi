; nsis-extra.nsi — included by Tauri's generated NSIS installer script.
; Tesseract and all its dependencies are now compiled statically via
; kreuzberg-tesseract, so no DLL bundling or cleanup is required.

!macro customInstall
!macroend

!macro customUninstall
!macroend
